import { AtencionDetalleEntidadRepository } from './atencion-detalle-entidad-repository';

describe('AtencionDetalleEntidadRepository', () => {
  it('should be defined', () => {
    expect(new AtencionDetalleEntidadRepository()).toBeDefined();
  });
});
