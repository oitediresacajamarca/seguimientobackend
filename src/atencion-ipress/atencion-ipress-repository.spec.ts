import { AtencionIpressRepository } from './atencion-ipress-repository';

describe('AtencionIpressRepository', () => {
  it('should be defined', () => {
    expect(new AtencionIpressRepository()).toBeDefined();
  });
});
