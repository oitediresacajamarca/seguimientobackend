import { DistribucionGeograficaRepository } from './distribucion-geografica.repository';

describe('DistribucionGeograficaRepository', () => {
  it('should be defined', () => {
    expect(new DistribucionGeograficaRepository()).toBeDefined();
  });
});
