import { DistribucionGeograficaEntity } from './distribucion-geografica.entity';

describe('DistribucionGeograficaEntity', () => {
  it('should be defined', () => {
    expect(new DistribucionGeograficaEntity()).toBeDefined();
  });
});
