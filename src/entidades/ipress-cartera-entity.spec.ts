import { IpressCarteraEntity } from './ipress-cartera-entity';

describe('IpressCarteraEntity', () => {
  it('should be defined', () => {
    expect(new IpressCarteraEntity()).toBeDefined();
  });
});
