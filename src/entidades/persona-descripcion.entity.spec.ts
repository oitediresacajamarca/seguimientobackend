import { PersonaDescripcionEntity } from './persona-descripcion.entity';

describe('PersonaDescripcionEntity', () => {
  it('should be defined', () => {
    expect(new PersonaDescripcionEntity()).toBeDefined();
  });
});
