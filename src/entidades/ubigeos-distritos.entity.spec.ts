import { UbigeosDistritosEntity } from './ubigeos-distritos.entity';

describe('UbigeosDistritosEntity', () => {
  it('should be defined', () => {
    expect(new UbigeosDistritosEntity()).toBeDefined();
  });
});
