import { ExamenesFisicosAtencionRepository } from './examenes-fisicos-atencion-repository';

describe('ExamenesFisicosAtencionRepository', () => {
  it('should be defined', () => {
    expect(new ExamenesFisicosAtencionRepository()).toBeDefined();
  });
});
