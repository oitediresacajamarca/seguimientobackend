import { HistoriaClinicaRepository } from './historia-clinica.repository';

describe('HistoriaClinicaRepository', () => {
  it('should be defined', () => {
    expect(new HistoriaClinicaRepository()).toBeDefined();
  });
});
