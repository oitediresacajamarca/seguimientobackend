import { IpressCarteraRepository } from './ipress-cartera-repository';

describe('IpressCarteraRepository', () => {
  it('should be defined', () => {
    expect(new IpressCarteraRepository()).toBeDefined();
  });
});
