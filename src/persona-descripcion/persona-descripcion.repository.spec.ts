import { PersonaDescripcionRepository } from './persona-descripcion.repository';

describe('PersonaDescripcionRepository', () => {
  it('should be defined', () => {
    expect(new PersonaDescripcionRepository()).toBeDefined();
  });
});
