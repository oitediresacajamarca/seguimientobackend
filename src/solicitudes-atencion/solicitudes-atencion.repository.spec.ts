import { SolicitudesAtencionRepository } from './solicitudes-atencion.repository';

describe('SolicitudesAtencionRepository', () => {
  it('should be defined', () => {
    expect(new SolicitudesAtencionRepository()).toBeDefined();
  });
});
