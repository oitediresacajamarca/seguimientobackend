import { TratamientoRepository } from './tratamiento.repository';

describe('TratamientoRepository', () => {
  it('should be defined', () => {
    expect(new TratamientoRepository()).toBeDefined();
  });
});
