import { UbigeosDistritosRepository } from './ubigeos-distritos.repository';

describe('UbigeosDistritosRepository', () => {
  it('should be defined', () => {
    expect(new UbigeosDistritosRepository()).toBeDefined();
  });
});
