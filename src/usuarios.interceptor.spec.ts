import { UsuariosInterceptor } from './usuarios.interceptor';

describe('UsuariosInterceptor', () => {
  it('should be defined', () => {
    expect(new UsuariosInterceptor()).toBeDefined();
  });
});
